{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "// rewrites": [
    "The gateway answers /health, /session and /chat. One function handles all three,",
    "so every path routes to it and the handler switches on the pathname."
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/gateway" }
  ]
}
